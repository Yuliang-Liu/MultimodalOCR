end2end_eval:
  metrics:
    text_block:
      metric:
        - Edit_dist
        # - BLEU
        # - METEOR
    display_formula:
      metric:
        - Edit_dist
        - CDM  # CDM can be calculated directly by calling CDM in config file if you have CDM environment
    table:
      metric:
        - TEDS
        - Edit_dist
    reading_order:
      metric:
        - Edit_dist
  dataset:
    dataset_name: end2end_dataset
    ground_truth:
      # data_path: demo_data/Multy-lingualOCRBench_damo.json
      data_path: /home/zhibolin/Multy-lingualOCRBench/demo_data/Multy-lingualOCRBench_private.json
    prediction:
      # data_path: demo_data/Gemini-3-pro-preview
      data_path: /home/zhibolin/Multy-lingualOCRBench/model_results/Gemini-3-pro-preview_private
    match_method: quick_match
    # filter: 
    #   language: english
